{{#if route.linkable}}
  {{#link-to route.path class=linkClass}}
    {{#if (has-block)}}
      {{yield this route}}
    {{else}}
      {{route.title}}
    {{/if}}
  {{/link-to}}
{{else}}
  {{#if (has-block)}}
    {{yield this route}}
  {{else}}
    {{route.title}}
  {{/if}}
{{/if}}
